# 2026-04-03 — Thursday

## Matrix Routing Fix — Third Time's the Charm

Bindings wiped AGAIN. This is the third time. Root cause identified this time:

**The correct location for bindings is the TOP-LEVEL `bindings` array**, NOT `channels.matrix.bindings`. The schema confirms `channels.matrix.bindings` doesn't exist in the OpenClaw schema — the gateway ignores anything placed there.

Previous fixes on April 2 put bindings in `channels.matrix.bindings` (wrong path). A later `config.patch` overwrote the top-level `bindings` array (which only had aic-pm from the April 3 agent setup). Result: all messages routed to Howard regardless of room.

**Fix applied:** 43 bindings in top-level `bindings` array. Gateway restarted.

**IMPORTANT:** Any future `config.patch` that touches `bindings` must include ALL 43 entries, not just new ones, because `config.patch` replaces arrays wholesale.

## Caroline / Julie — Partnership Context

Partner-pm's MEMORY.md has full details on both connections. Key update: Brian clarified "Angeline" was autocorrect — removed from memory. The Japan ambiguity (was it Caroline's partner or Mark's?) remains open — Brian leans toward it being Mark's based on Caroline's behavior (number exchange, physical escalation, warm LinkedIn response).
