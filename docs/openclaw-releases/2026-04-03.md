# OpenClaw Release Monitor — 2026-04-03

**Current installed version:** `2026.3.8` ⚠️ (version regression persists — see 2026-04-02 brief)
**Latest release:** `2026.4.2` (published Apr 2, 2026 ~18:30 UTC)
**Status:** Significantly behind — needs update

---

## 🔴 Action Needed

### Version Regression Still Present
Still on `2026.3.8` via Homebrew npm. The fnm-managed install is likely what the gateway daemon uses. **This needs Brian's attention** — check which node runtime the gateway daemon is using and update accordingly.

### Breaking: xAI x_search Config Moved
`tools.web.x_search.*` → `plugins.entries.xai.config.xSearch.*`. Fix with `openclaw doctor --fix`.
- **Impact on us:** Low unless we configured xAI search. Check after update.

### Breaking: Firecrawl web_fetch Config Moved
`tools.web.fetch.firecrawl.*` → `plugins.entries.firecrawl.config.webFetch.*`. Fix with `openclaw doctor --fix`.
- **Impact on us:** Low unless we use Firecrawl.

---

## 🟡 Worth Exploring

### Matrix Mentions Fix (#59323)
Matrix text sends now emit spec-compliant `m.mentions` metadata, so mentions actually notify in Element. This fixes a real pain point.

### Matrix Streaming Fix (#59384)
Live partial previews work correctly with `blockStreaming` enabled — completed blocks stay as separate messages while the current one updates in-place.

### Matrix Onboarding Restored (#59462)
`openclaw channels add` and `openclaw configure --section channels` guided setup for Matrix works again.

### Compaction Notification Toggle (#54251)
`agents.defaults.compaction.notifyUser` makes the "🧹 Compacting context..." message opt-in. Nice declutter.

### Plugin `before_agent_reply` Hook (#20067)
Plugins can now short-circuit the LLM with synthetic replies after inline actions. Opens up interesting plugin patterns.

### Task Flow System (multiple PRs)
Continued build-out of the Task Flow substrate — managed child task spawning, cancel intent propagation, plugin-owned Task Flow API. The `openclaw flows` CLI is maturing.

### Cron Timeout Visibility (#58247)
Timed-out exec/bash failures in isolated cron runs now surface even with `verbose: off`. Our cron jobs will report failures better.

---

## 🟢 FYI

### Security Hardening (massive batch)
- Provider transport policy centralized — request auth, proxy, TLS, headers unified across all HTTP/stream/websocket paths
- Anthropic/OpenAI/Copilot endpoint routing hardened against spoofing
- Exec env var overrides blocked for package roots, language runtimes, credential locations
- Workspace .env blocked from overriding pinned Python paths
- Webhook secret comparisons use timing-safe helpers across all channels
- Image generation SSRF policy tightened
- Gateway session kill requires proper auth scopes

### Bug Fixes Relevant to Us
- **Exec loopback restored** (#59092) — local exec/node clients stop failing with "pairing required" after 3.31
- **Subagent spawn fix** (#59555) — `sessions_spawn` no longer dies on loopback scope-upgrade
- **Anthropic thinking blocks stripped** (#59550) — `antml:thinking` tags no longer leak into replies
- **ACP gateway reconnects** (#59473) — prompts survive transient websocket drops
- **Exec approvals config sanitized** (#59112) — malformed policy enums fall back to defaults
- **WhatsApp push notifications restored** (#59410) — self-chat mode no longer kills push notifications
- **Telegram Allow Always button fix** (#59217) — approval IDs fit callback_data limit again

### Other Changes
- Android can launch OpenClaw from Google Assistant trigger
- Exec defaults now YOLO mode (security=full, ask=off) for gateway/node host
- QQ Bot channel hardening
- Mattermost probe SSRF guard

---

## Recommended Actions

1. **🔴 Fix version regression** — determine gateway's actual running version vs CLI version
2. **Update to `2026.4.2`** — significant security and stability improvements
3. **Run `openclaw doctor --fix`** — handles xAI and Firecrawl config migrations
4. **Set `agents.defaults.compaction.notifyUser: false`** to hide compaction notices
5. **Test Matrix mentions** after update — should notify properly in Element now
