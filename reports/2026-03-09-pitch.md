# ☀️ Morning Pitch — Monday, March 9, 2026

## The Idea
Install Agent Safehouse and become the first OpenClaw user to sandbox local AI agents — then document it for the community that's already asking.

## Why Now
Agent Safehouse hit 518 points on HN overnight — a single bash script that uses macOS's native kernel-enforced sandboxing to wrap any coding agent in a deny-first security model. An HN commenter literally asked "I'm wondering if this could be adapted for OpenClaw." You run 15 agents on your Mac Mini with full filesystem access. One bad prompt injection and an agent could `rm -rf ~` or read your tax documents. This is the security layer that's been missing from your setup. The SWE-CI benchmark (also from today's research) shows even the best coding agent regresses 25% of the time — meaning defensive infrastructure isn't paranoia, it's math.

## What It Looks Like
- `curl` one bash script, add shell functions to `.zshrc`
- Every Claude Code session runs kernel-sandboxed by default
- Agents can only access their working directory — SSH keys, personal files, other repos are blocked
- Interactive Policy Builder lets you generate custom profiles per agent
- Could adapt for OpenClaw's always-on agents (the HN discussion is already exploring this)

## Effort vs Impact
- **Effort:** Low — 15-minute install, test with one agent session
- **Impact:** High — Closes the biggest security gap in your setup, positions you as a contributor to the OpenClaw security conversation, and the write-up becomes a "Hull Report for AI builders" content piece

## Recommended Next Step
After the tax call today, spend 15 minutes installing Agent Safehouse and running one sandboxed Claude Code session. If it works, have Cyrus evaluate it for the full OpenClaw setup.

## Pillar Alignment
- 🤖 AI Agent Team (Pillar 2) — Security hardening for the entire agent fleet
- 💡 Building the Future (Pillar 6) — Content opportunity, community positioning
